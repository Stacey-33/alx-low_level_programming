#include <stdio.h>
/**
 * main - A program that prints lowercase alphabets in reverse
 * Return: 0 (Success)
 */
int main(void)
{
	char c;

	char = 'z';
	while (c >= 'a')
	{
		putchar(c);
		c--;
	}
	putchar('\n')
	return (0);
}
