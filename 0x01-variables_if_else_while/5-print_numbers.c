#include <stdio.h>
/**
 * main - A program that prints single digits starting from 0
 * Return: 0 (Success)
 */
int main(void)
{
	int a;
	for (a = 0; a <= 10; a++)
		putchar(a);
	putchar('\n');
	return (0);
}
