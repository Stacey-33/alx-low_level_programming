#include "main.h"
/**
 * main - A prgram that prints 10x the alphabet
 * Return: 0 (Success)
 */
void print_alphabet_x10(void)
{
	char i, k;

	for (k = 0; k < 10; k++)
	{
		_putchar(i);
	}
	_putchar('\n');
	return (0);
}
